class Point is
  var x : Int;
  var y : Int;
  
  method init(a : Int, b : Int) : Void =>
    x := a;
    y := b
    
  method print() : Void =>
    output(x);
    output(y)
end

class Main is
  var p : Point;
  method main() : Void =>
    p.init(3, 4);
    p.print()
end