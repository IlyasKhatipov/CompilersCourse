class Point is
  var x : Integer
  var y : Integer
  
  this(xVal: Integer, yVal: Integer) is
    x := xVal
    y := yVal
  end
  
  method add(other: Point) : Point is
    return Point(x.Plus(other.x), y.Plus(other.y))
  end
end