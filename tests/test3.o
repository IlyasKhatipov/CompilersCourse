class Main is
  var x : Int;
  method main() : Void =>
    x := 5;
    if x < 0 then
      output("neg");
    else if x == 0 then
      output("zero");
    else if x < 10 then
      output("small");
    else
      output("big")
    end
end
