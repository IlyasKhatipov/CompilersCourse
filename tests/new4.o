class Main is
  this() is
    var n : Integer(5)
    var s : Integer(0)
    var i : Integer(1)
    while i.LessEqual(n) loop
      s := s.Plus(i)
      i := i.Plus(Integer(1))
    end
    output(s)
  end
end
